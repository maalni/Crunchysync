variables:
  DOCKER_DRIVER: overlay2

image: maalni/maalni.gitlab.io:latest

stages:
  - build

cache:
  paths:
  - node_modules/

build:
  stage: build
  before_script:
  - yarn
  script:
  - ng build --prod --build-optimizer
  only:
  - master
  artifacts:
    paths:
    - dist/
